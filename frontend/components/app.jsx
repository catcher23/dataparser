var React = require('react');
import Interface from './interface.jsx';
module.exports = React.createClass({

  render: () => {
    return (
      <div>
        <Interface />
      </div>
    );
  }
});
